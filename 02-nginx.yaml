- name: install and run nginx
  hosts: web
  tasks:
  - name: install nginx package
    ansible.builtin.package:
      name: nginx
      state: present
  - name: run nginx
    ansible.buitin.service:
      name: nginx 
      state: started